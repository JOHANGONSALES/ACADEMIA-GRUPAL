{% form_theme form 'bootstrap_4_layout.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary" style="margin-bottom: 10px; width: 80px;">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
