{% extends 'base.html.twig' %}

{% block title %}Alumnoscursos{% endblock %}

{% block body %}
    <h1>Alumnoscursos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ alumnoscurso.id }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ alumnoscurso.fecha ? alumnoscurso.fecha|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_alumnoscursos_index') }}">back to list</a>

    <a href="{{ path('app_alumnoscursos_edit', {'id': alumnoscurso.id}) }}">edit</a>

    {{ include('alumnoscursos/_delete_form.html.twig') }}
{% endblock %}
