{% extends 'base.html.twig' %}

{% block title %}Alumnos - cursos{% endblock %}

{% block div %}
<section class="section" id="about">
        <div class="container">
    <h1>Alumnos - cursos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ alumnoscurso.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ alumnoscurso.FkAlumno.nombre }} {{ alumnoscurso.FkAlumno.primerApellido }}</td>
            </tr>
            <tr>
                <th>Curso</th>
                <td>{{ alumnoscurso.FkCurso.titulo }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ alumnoscurso.fecha ? alumnoscurso.fecha|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>
    {{ include('alumnoscursos/_delete_form.html.twig') }}
    <a href="{{ path('app_alumnoscursos_edit', {'id': alumnoscurso.id}) }}" class="btn btn-outline-primary">edit</a>
    <div style="margin-top: 20px;">
        <a href="{{ path('app_alumnoscursos_index') }}" class="btn btn-outline-primary">back to list</a>
    </div>
    </div>
</section>
{% endblock %}
