<form method="post" action="{{ path('app_alumnoscruso_delete', {'id': alumnoscruso.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ alumnoscruso.id) }}">
    <button class="btn">Delete</button>
</form>
