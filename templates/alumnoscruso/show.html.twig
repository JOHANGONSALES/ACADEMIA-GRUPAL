{% extends 'base.html.twig' %}

{% block title %}Alumnoscruso{% endblock %}

{% block body %}
    <h1>Alumnoscruso</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ alumnoscruso.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_alumnoscruso_index') }}">back to list</a>

    <a href="{{ path('app_alumnoscruso_edit', {'id': alumnoscruso.id}) }}">edit</a>

    {{ include('alumnoscruso/_delete_form.html.twig') }}
{% endblock %}
