{% extends 'base.html.twig' %}

{% block title %}Edit Curso{% endblock %}

{% block div %}
    <div style="margin: 50px 200px;">
        <h1>Edit Curso</h1>

        {% form_theme form 'bootstrap_4_layout.html.twig' %}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-outline-primary" style="margin-bottom: 10px; width: 80px;">{{ button_label|default('Update') }}</button>
        {{ form_end(form) }}
        {{ include('cursos/_delete_form.html.twig') }} 
       
    </div>
   
   <a href="{{ path('app_cursos_index') }}" style="font-weight: 600; margin: 0 200px;">Back to list</a>  
    
{% endblock %}

